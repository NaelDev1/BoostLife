<UserControl x:Class="BoostLife.CustomControls.CreditCardBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:BoostLife.CustomControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Border CornerRadius="20" BorderBrush="#fff"  BorderThickness="4"
                Width="350" Height="180">

            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="{Binding PrimaryColor, RelativeSource={RelativeSource AncestorType=UserControl}}" Offset="0"/>
                    <GradientStop Color="{Binding SecondaryColor, RelativeSource={RelativeSource AncestorType=UserControl}}" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <icon:PackIconBootstrapIcons Kind="CreditCard" Grid.Column="1" Margin="10" Width="49" Height="25" />

                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="#000" FontWeight="Bold" FontSize="15" Margin="10" />
                <StackPanel Grid.Row="1" Margin="10">
                    <TextBlock Text="FATURA DO MÊS" Foreground="#000" FontWeight="Bold" FontSize="15" />
                    <TextBlock Margin="0 5 0 0 " Text="{Binding Amount, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="#000" FontWeight="Bold" FontSize="30"/>

                </StackPanel>

                <TextBlock Grid.Row="2" Grid.ColumnSpan="2"  Foreground="#000" FontWeight="Bold" FontSize="15" Margin="10 10 15 10" HorizontalAlignment="Right">
                    <Run Text="Fecha dia"/>
                    <Run Text="{Binding CloseDay, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                </TextBlock>
            </Grid>

        </Border>

    </Grid>
</UserControl>
