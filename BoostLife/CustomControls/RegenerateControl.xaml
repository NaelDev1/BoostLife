<UserControl x:Class="BoostLife.CustomControls.RegenerateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:BoostLife.CustomControls"
             mc:Ignorable="d"
             MinWidth="400">
    <UserControl.Resources>
        <Style x:Key="CustomProgress" TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid  >
                            <!--<Border CornerRadius="15" Background="#452c63" />-->
                            <Border x:Name="PART_Track" CornerRadius="15">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#1d1160"/>
                                        <GradientStop Color="#452c63" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    CornerRadius="10"
                                    Margin="5"
                                    >
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#720e9e"/>
                                        <GradientStop Color="#EE82EE" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>

                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    <Grid>
        <Border  CornerRadius="20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#202020"/>
                    <GradientStop Color="#101010" Offset="1"/>

                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Alcool" Foreground="#cbcbcb" FontWeight="Black" Margin="10 0 0 0" FontSize="20" VerticalAlignment="Bottom"/>
                <ProgressBar Value="50" Style="{StaticResource CustomProgress}" Grid.Row="1" Margin="5" Height="40"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <TextBlock Text="Livre há" Foreground="#cbcbcb" FontWeight="Medium" FontSize="15"/>
                    <TextBlock Text="10d 11m 47s" Foreground="#cbcbcb" FontWeight="Medium" FontSize="15"  Margin="20 0 10 0"/>

                </StackPanel >
                <StackPanel Grid.Column="1" Grid.RowSpan="2" Margin="5" Orientation="Horizontal">

                    <icon:PackIconBootstrapIcons Kind="Clock" Width="60" Height="40" Margin="0" Foreground="white" HorizontalAlignment="Stretch"
                                                 VerticalAlignment="Bottom"/>

                </StackPanel >


            </Grid>

        </Border>

    </Grid>



</UserControl>
